@page
@model Dajeej.Areas.Admin.Pages.SocialLinks.IndexModel
@{
}

<div class="page-content">
    <div class="container-fluid">

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h2 class="card-header d-flex justify-content-between align-items-center">
                            @sharedResource["Social Media Links"]
                        </h2>
                    </div>
                    <div class="card-body">
                        @(Html.DevExtreme().DataGrid<Dajeej.Models.SoicialMidiaLink>()
.DataSource(ds => ds.Mvc()
.Controller("SoicialMidiaLinks")
.LoadAction("Get")
.UpdateAction("Put")
.Key("id")
)
.RemoteOperations(true)
.Columns(columns => {
    columns.AddFor(m => m.facebooklink).Caption(sharedResource["Facebook"]);
    columns.AddFor(m => m.TwitterLink).Caption(sharedResource["Twitter"]);
     columns.AddFor(m => m.Instgramlink).Caption(sharedResource["Instgram"]);
     columns.AddFor(m => m.LinkedInlink).Caption(sharedResource["LinkedIn"]);
     columns.AddFor(m => m.WhatsApplink).Caption(sharedResource["WhatsApp"]);

}

)

.Editing(e => e.Mode(GridEditMode.Popup)
.Texts(c => c.CancelRowChanges(sharedResource["Cancel"]) .SaveRowChanges(sharedResource["Save"]) .EditRow(sharedResource["Edit"]).AddRow(sharedResource["Add"]).ConfirmDeleteMessage(sharedResource["Are you sure to delete this record ?"]))




.AllowUpdating(true)
.UseIcons(true)


.Popup(p => p
.Title(sharedResource["Social Media Links"])
.ShowTitle(true)
.MaxWidth(750)
.RtlEnabled(bool.Parse(sharedResource["ArLang"]))


.Height(300))

)
    .Paging(paging => paging.PageSize(10))
                        .Pager(pager =>
                        {
                            pager.Visible(true);
                            pager.DisplayMode(GridPagerDisplayMode.Full);
                            pager.ShowPageSizeSelector(true);
                            pager.AllowedPageSizes(new JS("[5, 10, 'all']"));
                            pager.ShowInfo(true);
                            pager.ShowNavigationButtons(true);

                        })
        .HeaderFilter(headerFilter => headerFilter.Visible(true))
                            .ShowRowLines(true).ShowColumnLines(true)
                            .RowAlternationEnabled(true)
                            .AllowColumnReordering(true)
                            .ShowColumnLines(true)
                            .ShowBorders(true)
                            .SearchPanel(searchPanel => searchPanel
                            .Visible(true)
                            .Width(240)
                            .Placeholder(sharedResource["SearchTxt"]))
                            .RemoteOperations(true)
                            .RtlEnabled(bool.Parse(sharedResource["ArLang"]))
)

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>